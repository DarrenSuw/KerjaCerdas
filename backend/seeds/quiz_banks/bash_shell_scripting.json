{
  "skill": "Bash/Shell Scripting",
  "skill_key": "bash/shell scripting",
  "questions": [
    {
      "question": "Anda perlu memantau log aplikasi secara real-time dan mencari baris yang mengandung kata 'ERROR' atau 'FATAL' untuk keperluan troubleshooting cepat di server production. Perintah bash apa yang paling efisien digunakan?",
      "options": [
        "cat /var/log/app.log | grep -E 'ERROR|FATAL'",
        "tail -f /var/log/app.log | grep -E 'ERROR|FATAL'",
        "less +F /var/log/app.log | find 'ERROR'",
        "awk '/ERROR|FATAL/' /var/log/app.log"
      ],
      "correct_index": 1
    },
    {
      "question": "Sebuah script backup otomatis gagal berjalan karena masalah hak akses pada direktori tujuan. Variabel mana yang paling tepat untuk memeriksa status exit code dari perintah backup terakhir di dalam bash script?",
      "options": [
        "echo $status",
        "echo $PID",
        "echo $?",
        "echo $EX"
      ],
      "correct_index": 2
    },
    {
      "question": "Anda diminta membuat script migrasi file yang memproses daftar nama file dari sebuah variabel string. Karakter pemisah (IFS) default sering membuat spasi dalam nama file menjadi masalah. Cara terbaik mengatur IFS di dalam script adalah:",
      "options": [
        "IFS=$'\\n'; set -f",
        "IFS=\" \"; export IFS",
        "IFS=':' unset IFS",
        "IFS=$'\\n\\t'"
      ],
      "correct_index": 0
    },
    {
      "question": "Dalam skenario CI/CD, script deployment Anda sering gagal secara diam-diam karena perintah sebelum pipa (pipe) mengalami error, tetapi pipe secara default mengembalikan exit status dari perintah terakhir. Bagaimana cara membuat bash script mendeteksi error di tengah pipa?",
      "options": [
        "Gunakan perintah set -x di awal script",
        "Gunakan perintah set -e dan set -o pipefail",
        "Tambahkan trap 'echo error' ERR",
        "Gunakan pengecekan if [ $? -ne 0 ]"
      ],
      "correct_index": 1
    },
    {
      "question": "Anda sedang menulis script untuk melakukan rotasi log. Anda perlu mengekstrak tanggal dari nama file 'access.log.2023-10-25' menggunakan command substitution standar. Perintah mana yang paling tepat?",
      "options": [
        "date=$(echo 'access.log.2023-10-25' | cut -d'.' -f3)",
        "date=$(echo 'access.log.2023-10-25' | awk -F'.' '{print $3}')",
        "date=$(basename 'access.log.2023-10-25' | sed 's/access.log.//')",
        "date=$(expr match 'access.log.2023-10-25' '.*\\(.*\\)')"
      ],
      "correct_index": 2
    },
    {
      "question": "Script deployment Anda perlu membaca konfigurasi dari file eksternal 'config.env' yang berisi pasangan kunci-nilai (KEY=VALUE). Cara paling aman dan standar POSIX untuk memuat file tersebut ke dalam environment script adalah:",
      "options": [
        "source ./config.env",
        "import ./config.env",
        "eval $(cat ./config.env)",
        "exec < ./config.env"
      ],
      "correct_index": 0
    },
    {
      "question": "Anda ingin menulis loop dalam bash script untuk memproses 100 file log bernomor dari log_001.log hingga log_100.log. Sintaks loop C-style di bash yang paling tepat adalah:",
      "options": [
        "for i in {1..100}; do process log_$i.log; done",
        "for ((i=1; i<=100; i++)); do process log_$(printf '%03d' $i).log; done",
        "while [ $i -le 100 ]; do process log_$i.log; i=$((i+1)); done",
        "foreach i in $(seq 1 100); do process log_$i.log; done"
      ],
      "correct_index": 1
    },
    {
      "question": "Sebuah script cron job berjalan setiap malam dan menghasilkan banyak output teks yang tidak diinginkan ke terminal/log sistem. Bagaimana cara mengarahkan standard output dan standard error dari sebuah perintah ke null device agar bersih?",
      "options": [
        "command > /dev/null 2>&1",
        "command 2>&1 > /dev/null",
        "command &> /dev/null",
        "command | /dev/null"
      ],
      "correct_index": 0
    },
    {
      "question": "Dalam script monitoring kapasitas disk, Anda mendapatkan nilai persentase penggunaan dari perintah df, misalnya '85%'. Anda ingin mengambil angka 85 saja sebagai integer untuk dibandingkan dengan batas threshold. Perintah sed mana yang paling akurat?",
      "options": [
        "echo '85%' | sed 's/%//'",
        "echo '85%' | sed 's/[^0-9]//g'",
        "echo '85%' | sed 's/\\([0-9]*\\)%/\\1/'",
        "echo '85%' | sed 's/\\%//g'"
      ],
      "correct_index": 1
    },
    {
      "question": "Anda perlu menghentikan proses yang sedang berjalan pada port 8080 di server Linux produksi Anda secara cepat dari terminal. Perintah kombinasi mana yang paling efektif dan aman digunakan?",
      "options": [
        "kill -9 $(ps aux | grep 8080)",
        "fuser -k 8080/tcp",
        "pkill -f http-8080",
        "systemctl stop port-8080"
      ],
      "correct_index": 1
    },
    {
      "question": "Anda membuat script otomatisasi yang menerima argumen path direktori dari pengguna. Bagaimana cara script memvalidasi apakah argumen yang diberikan benar-benar sebuah direktori yang ada?",
      "options": [
        "if [ -d \"$1\" ]; then",
        "if [ -f \"$1\" ]; then",
        "if [ -e \"$1\" ]; then",
        "if [ -r \"$1\" ]; then"
      ],
      "correct_index": 0
    },
    {
      "question": "Saat menjalankan script untuk memproses file teks berukuran besar, operasi string manipulation ${var//old/new} berjalan sangat lambat di bash murni. Alat baris perintah mana yang lebih tepat dipanggil di dalam script untuk performa tinggi?",
      "options": [
        "awk atau sed",
        "grep dan tr",
        "cat dan head",
        "find dan xargs"
      ],
      "correct_index": 0
    },
    {
      "question": "Anda perlu menulis variabel lingkungan (environment variables) dari sebuah script ke file `.env`, namun ingin memastikan variabel tersebut tidak menimpa isi file yang sudah ada, melainkan menambahkannya di baris baru. Operator apa yang harus digunakan?",
      "options": [
        "Menggunakan operator > pada file tujuan",
        "Menggunakan operator >> pada file tujuan",
        "Menggunakan operator < pada file tujuan",
        "Menggunakan operator 2>&1 pada file tujuan"
      ],
      "correct_index": 1
    },
    {
      "question": "Sebuah script backup Anda memakan waktu lama dan Anda ingin menjalankannya di background agar terminal bisa ditutup tanpa mematikan proses script tersebut. Perintah apa yang paling tepat?",
      "options": [
        "nohup ./backup.sh &",
        "screen -d -m ./backup.sh",
        "bg && disown ./backup.sh",
        "tmux new-session -d './backup.sh'"
      ],
      "correct_index": 0
    },
    {
      "question": "Anda ingin menghitung total ukuran file (dalam bytes) dari semua file berformat `.log` di dalam direktori saat ini menggunakan kombinasi perintah bash. Manakah pipa perintah yang paling akurat?",
      "options": [
        "find . -name '*.log' -exec du -b {} + | awk '{sum+=$1} END {print sum}'",
        "ls -l *.log | awk '{sum+=$5} END {print sum}'",
        "du -sh *.log | awk '{sum+=$1} END {print sum}'",
        "cat *.log | wc -c"
      ],
      "correct_index": 1
    },
    {
      "question": "Dalam script deployment, Anda perlu memastikan bahwa sebuah layanan HTTP merespon dengan status 200 sebelum melanjutkan tahap berikutnya. Perintah curl apa yang paling efisien untuk mengecek status HTTP saja?",
      "options": [
        "curl -I -s -o /dev/null -w \"%{http_code}\" http://localhost",
        "curl -s http://localhost | grep \"200 OK\"",
        "curl -X GET http://localhost --status",
        "curl --check-status 200 http://localhost"
      ],
      "correct_index": 0
    },
    {
      "question": "Anda mendapati script bash Anda berperilaku aneh karena ada variabel yang digunakan tanpa sengaja belum pernah didefinisikan sebelumnya. Opsi bash apa yang bisa mengaktifkan error ketika variabel tak terdefinisi dipakai?",
      "options": [
        "set -o nounset",
        "set -o errexit",
        "set -o noundefined",
        "set -o strict"
      ],
      "correct_index": 0
    },
    {
      "question": "Anda ingin melakukan iterasi pada setiap baris dari sebuah file teks `user_list.txt` di dalam bash script, dan menghindari masalah jika ada spasi pada nama pengguna. Pendekatan `while read` mana yang benar?",
      "options": [
        "while read -r line; do echo \"$line\"; done < user_list.txt",
        "for line in $(cat user_list.txt); do echo \"$line\"; done",
        "cat user_list.txt | while read line; do echo $line; done",
        "foreach line in user_list.txt; do echo $line; done"
      ],
      "correct_index": 0
    },
    {
      "question": "Anda ingin menjalankan sekumpulan perintah sistem secara paralel di latar belakang dalam script backup Anda, namun perlu membatasi agar maksimal hanya 4 proses yang berjalan bersamaan agar server tidak kehabisan memori. Pendekatan apa yang paling tepat?",
      "options": [
        "Menggunakan operator '&' di akhir setiap perintah lalu memanggil 'wait' di akhir script",
        "Membuat loop dengan GNU 'parallel' atau memanfaatkan antrean berbasis job control '&' dan 'wait -n'",
        "Menjalankan setiap perintah di dalam fungsi terpisah dengan pemanggilan 'nohup' secara berurutan",
        "Mengatur variabel lingkungan BASH_MAX_JOBS sebelum menjalankan perintah dengan ampersand"
      ],
      "correct_index": 1
    },
    {
      "question": "Sebuah script deployment Anda membaca daftar server target dari file teks. Baris terakhir dari file tersebut kadang tidak diakhiri dengan newline (karakter enter), sehingga pembacaan baris terakhir sering terlewat oleh perintah 'read'. Bagaimana cara memperbaikinya?",
      "options": [
        "Menggunakan opsi '-r' pada perintah read dan memastikan loop memeriksa status exit dari read",
        "Menambahkan karakter newline secara otomatis menggunakan perintah sed sebelum loop dijalankan",
        "Mengubah struktur loop menggunakan perulangan for dengan pemisahan IFS khusus file teks",
        "Menggunakan perintah dos2unix untuk membersihkan format baris pada file teks target"
      ],
      "correct_index": 0
    },
    {
      "question": "Dalam script automasi CI/CD, Anda perlu memastikan bahwa script segera berhenti dan keluar jika ada salah satu perintah yang gagal, termasuk perintah di dalam fungsi. Pengaturan shell apa yang harus diletakkan di awal script?",
      "options": [
        "set -e -u -o pipefail dan memastikan penangkapan error di setiap blok kode",
        "set -o errexit -o nounset dan memastikan fungsi dideklarasikan dengan benar",
        "set -x untuk debugging dan set -e agar script langsung berhenti saat ada error",
        "set -euo pipefail di baris paling atas setelah shebang untuk penanganan error ketat"
      ],
      "correct_index": 3
    },
    {
      "question": "Anda sedang menulis script pembersih direktori temporary. Anda ingin menghapus file apa pun yang berakhiran '.tmp' di dalam direktori '/var/app/tmp' yang akses terakhirnya lebih dari 7 hari lalu, namun aman terhadap spasi pada nama file. Perintah apa yang paling tepat?",
      "options": [
        "find /var/app/tmp -type f -name '*.tmp' -mtime +7 -exec rm -f {} +",
        "rm -f /var/app/tmp/*.tmp -mtime +7 dengan menggunakan opsi direktori aman",
        "ls /var/app/tmp | grep '.tmp$' | xargs -I {} rm -f /var/app/tmp/{}",
        "find /var/app/tmp -maxdepth 1 -name '*.tmp' -ctime +7 -delete"
      ],
      "correct_index": 0
    },
    {
      "question": "Anda ingin mengirimkan payload JSON ke webhook internal perusahaan menggunakan curl di dalam bash script, di mana data JSON disimpan dalam variabel string bash. Bagaimana cara memastikan karakter khusus dalam variabel aman dikirim?",
      "options": [
        "Menggunakan jq untuk menyusun ulang struktur data sebelum diteruskan ke argumen curl",
        "Menggunakan opsi --data-raw dengan variabel yang dibungkus tanda kutip ganda",
        "Menggunakan opsi -d dengan format string langsung tanpa escape karakter tambahan",
        "Menggunakan jq -n --arg var \"$val\" '$var' untuk melakukan escape otomatis yang aman"
      ],
      "correct_index": 3
    },
    {
      "question": "Dalam sebuah script migrasi data, Anda perlu membaca file CSV yang memiliki baris header di awal. Anda ingin mengabaikan baris pertama tersebut saat melakukan iterasi data baris demi baris. Bagaimana cara paling efisien dalam bash?",
      "options": [
        "Menggunakan perintah 'tail -n +2' pada file input sebelum dimasukkan ke dalam loop pembacaan",
        "Menambahkan kondisi penomoran baris di dalam loop menggunakan variabel counter internal",
        "Menggunakan perintah 'sed 1d' pada file input untuk membuang header sebelum diproses",
        "Menggunakan opsi pembacaan khusus pada perintah read untuk melompati baris pertama"
      ],
      "correct_index": 0
    },
    {
      "question": "Anda ingin memastikan bahwa script bash Anda hanya dapat dijalankan oleh pengguna dengan hak akses root (UID 0), dan jika bukan root, script harus memberikan pesan error dan keluar dengan status non-nol. Baris kode mana yang paling tepat?",
      "options": [
        "if [ $(whoami) != 'root' ]; then echo 'Harus root'; exit 1; fi",
        "if [ $EUID -ne 0 ]; then echo 'Script ini harus dijalankan sebagai root' >&2; exit 1; fi",
        "if [ $UID -eq 0 ]; then echo 'Berhasil'; else exit 1; fi",
        "if [ $(id -u) -ne 0 ]; then echo 'Akses ditolak'; return 1; fi"
      ],
      "correct_index": 1
    },
    {
      "question": "Anda memiliki script yang menerima parameter opsional menggunakan 'getopts'. Anda ingin menangani argumen '-h' untuk bantuan dan '-v' untuk verbose mode. Struktur dasar loop mana yang paling standar dan aman?",
      "options": [
        "while getopts 'hv' opt; do case $opt in h) ... ;; v) ... ;; esac; done",
        "for arg in \"$@\"; do case $arg in -h) ... ;; -v) ... ;; esac; done",
        "while [ $# -gt 0 ]; do case $1 in -h) ... ;; -v) ... ;; esac; shift; done",
        "until [ -z \"$1\" ]; do getopts 'hv' opt; case $opt in ...) ... ;; esac; done"
      ],
      "correct_index": 0
    },
    {
      "question": "Anda ingin mencetak waktu saat ini dalam format 'YYYY-MM-DD_HH-MM-SS' untuk digunakan sebagai penamaan file arsip backup. Perintah date manakah yang menghasilkan format tersebut secara akurat?",
      "options": [
        "date +\"%Y-%m-%d_%H-%M-%S\"",
        "date +\"%Y%m%d-%T\"",
        "date +\"%Y-%D-%T\"",
        "date +\"%F_%T\""
      ],
      "correct_index": 0
    },
    {
      "question": "Dalam script pemantauan server, Anda perlu memeriksa apakah suatu perintah eksternal (misalnya 'pg_isready') tersedia di sistem sebelum script melanjutkan eksekusi utama. Perintah mana yang paling tepat digunakan?",
      "options": [
        "test -x $(which pg_isready)",
        "command -v pg_isready >/dev/null 2>&1",
        "type pg_isready && exit 0",
        "which pg_isready &>/dev/null"
      ],
      "correct_index": 1
    },
    {
      "question": "Anda sedang membuat script otomatisasi untuk melakukan deployment. Anda ingin memastikan bahwa variabel PATH sistem di dalam script bersih dan mendefinisikannya secara eksplisit pada baris awal. Format manakah yang paling tepat untuk mendefinisikan PATH di awal script bash?",
      "options": [
        "PATH=\"/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin\"",
        "export PATH=\"/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin\"",
        "set PATH=\"/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin\"",
        "declare PATH=\"/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin\""
      ],
      "correct_index": 1
    },
    {
      "question": "Dalam script monitoring server, Anda ingin memeriksa apakah suatu port tertentu (misalnya 3306) sedang mendengarkan koneksi menggunakan utilitas 'nc' atau 'netcat'. Perintah kondisional manakah yang paling aman dan tepat digunakan dalam bash?",
      "options": [
        "if nc -z localhost 3306; then echo \"Port terbuka\"; fi",
        "if [ nc -z localhost 3306 == 0 ]; then echo \"Port terbuka\"; fi",
        "if test nc -z localhost 3306 = true; then echo \"Port terbuka\"; fi",
        "if [ $(nc -z localhost 3306) ]; then echo \"Port terbuka\"; fi"
      ],
      "correct_index": 0
    }
  ]
}